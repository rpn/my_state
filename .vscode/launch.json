{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run rtom command",
      "type": "node-terminal",
      "request": "launch",
      "command": "rtom -f build -e '$run()'",
      "cwd": "${workspaceFolder}"
    }
  ]
}
